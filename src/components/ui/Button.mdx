import {
  Canvas,
  Description,
  Meta,
  Source,
  Subtitle,
  Title,
} from '@storybook/addon-docs/blocks';
import * as ButtonStories from './__stories__/Button.stories';
import { Button } from './Button';

<Meta title='UI/Button/Docs' of={ButtonStories} />
<Title>Button</Title>
<Subtitle>Primary actions for your UI</Subtitle>

<Description>
  Use Button for primary & secondary actions. Variants: <code>default</code>,{' '}
  <code>outline</code>,<code>ghost</code>, <code>destructive</code>. Sizes:{' '}
  <code>sm</code>, <code>md</code>, <code>lg</code>, <code>icon</code>.
</Description>

## Example

<Canvas of={ButtonStories.Primary} />

## Source

<Source of={ButtonStories.Primary} />
